/// Font families — `docs/ui-ux-system/02-design-tokens.md` §2.
abstract final class AppFonts {
  /// Sans-serif chính (mobile + web).
  static const String fontFamily = 'Inter';

  /// Phiên âm IPA / ký tự đặc biệt.
  static const String fontFamilyIpa = 'NotoSans';

  /// Legacy — không dùng cho UI mới.
  static const String lexend = 'Lexend';
}
