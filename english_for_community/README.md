# Flutter app — English for Community

Tài liệu đầy đủ về toàn bộ monorepo (kiến trúc, backend, tính năng, cách chạy) nằm ở **[README gốc trong thư mục trên một cấp](../README.md)**.

## Chạy nhanh

```bash
flutter pub get
flutter run
```

Đảm bảo API backend đang chạy và cấu hình endpoint / Firebase khớp môi trường của bạn.
