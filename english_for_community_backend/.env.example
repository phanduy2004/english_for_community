# MongoDB (bắt buộc trên Render)
MONGODB_URI=mongodb+srv://USER:PASS@cluster.mongodb.net/english_for_community

# JWT
JWT_SECRET=your-secret
JWT_REFRESH_SECRET=your-refresh-secret

# Các biến khác (Cloudinary, Firebase, email...) — xem dashboard Render hiện tại
